apply plugin: "war"

bootWar {
    mainClass = "online.shop.web.OnlineShopApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
